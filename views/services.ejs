<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Our Services</title>
    <style>
      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background: #f8fafc;
      }

      .services-section {
        padding: 80px 0;
        background: linear-gradient(135deg, #0f172a, #1e293b);
        color: #fff;
      }

      .services-section h2 {
        font-weight: bold;
        margin-bottom: 15px;
        text-align: center;
      }

      .services-section p.lead {
        text-align: center;
        margin-bottom: 50px;
        color: #cbd5e1;
      }

      .card {
        border: none;
        border-radius: 15px;
        overflow: hidden;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        background: #fff;
      }

      .card:hover {
        transform: translateY(-10px);
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
      }

      .card img {
        height: 180px;
        object-fit: cover;
      }

      .card-body {
        padding: 20px;
      }

      .card-title {
        font-weight: 700;
        color: #0f172a;
      }

      .card-text {
        color: #475569;
        font-size: 15px;
        min-height: 60px;
      }

      .btn-service {
        margin-top: 10px;
        background: #0f172a;
        color: #fff;
        border-radius: 8px;
        transition: 0.3s;
      }

      .btn-service:hover {
        background: #1e40af;
      }

      .service-icon {
        font-size: 40px;
        color: #0f172a;
        background: #e2e8f0;
        border-radius: 50%;
        width: 70px;
        height: 70px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: -50px auto 20px auto;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
      }
    </style>
  </head>
  <body>
    <%- include('./base/header') %>
    <%- include('./base/navbar') %>
    <section class="services-section">
      <div class="container">
        <h2>🚀 Our Services</h2>
        <p class="lead">
          We provide modern IT solutions that empower your business to grow and
          scale effectively.
        </p>
        <div class="row g-4">
          <% for( let data of alldata ) { %>
          <div class="col-md-4">
            <div class="card h-100 text-center">
              <img src="<%= data.image %>" alt="<%= data.title %>" />
              <div class="service-icon">
                <i class="bi bi-layers"></i>
              </div>
              <div class="card-body">
                <h5 class="card-title"><%= data.title %></h5>
                <p class="card-text"><%= data.description %></p>
                <a href="/services" class="btn btn-service">Learn More</a>
              </div>
            </div>
          </div>
         <% }%>
        </div>
      </div>
    </section>
    <%- include('./base/footer') %>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
